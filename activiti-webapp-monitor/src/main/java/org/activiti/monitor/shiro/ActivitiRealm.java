package org.activiti.monitor.shiro;

public class ActivitiRealm /*
							 * extends SimpleAccountRealm
							 */
{
	/*
	 * @Inject ProcessEngine processEngine;
	 * 
	 * @Override protected AuthenticationInfo
	 * doGetAuthenticationInfo(AuthenticationToken token) throws
	 * AuthenticationException { UsernamePasswordToken upToken =
	 * (UsernamePasswordToken) token; if (!accountExists(upToken.getUsername()))
	 * { throw new UnknownAccountException("Unknown account" +
	 * upToken.getUsername()); }
	 * 
	 * return super.doGetAuthenticationInfo(token);
	 * 
	 * }
	 */

}
